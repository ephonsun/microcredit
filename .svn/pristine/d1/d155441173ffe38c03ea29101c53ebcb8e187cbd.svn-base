package banger.framework.component.dataexport;

import java.util.List;

public interface IExcelSheet {
	
	/**
	 * 行数
	 * @return
	 */
	int rowSize();
	
	/**
	 * 向Excel工作表添加数据
	 * @param row
	 */
	void addRow(Object[] row);
	
	/**
	 * 向Excel工作表添加数据
	 * @param rows 多行数据
	 */
	void addRows(List<Object[]> rows);
	
	/**
	 * 向Excel工作表添加数据
	 * @param rows 多行数据
	 */
	void addRows(Object[][] rows);
	
}
